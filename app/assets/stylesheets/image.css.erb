.top-container {
  background-image: url("<%= asset_path('top-image.jpg') %>");
  background-size: cover;
  height: 100%;
  min-height: 960px;
}

.tasks-container {
  background-image: url("<%= asset_path('tasks-image.jpg') %>");
  background-size: cover;
  height: 100%;
  min-height: 960px;
}

.users-container {
  background-image: url("<%= asset_path('users-image.jpg') %>");
  background-size: cover;
  height: 100%;
  min-height: 960px;
}
